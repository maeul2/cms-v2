/*
package kr.co.yna.cms.v2.yna.authority.domain.repository;

import kr.co.yna.cms.v2.yna.authority.domain.entity.Authority;
import org.springframework.data.repository.Repository;

import java.util.Collection;
import java.util.Optional;
import java.util.Set;

@org.springframework.stereotype.Repository
public interface AuthorityRepository extends Repository<Authority, Long> {
    Authority save(Authority authority);

    Set<Authority> findByIdIn(Collection<Long> ids);

    Set<Authority> findAll();

    Optional<Authority> findById(long id);
}
*/
